---
/**
 * FOUNDATION PLACEHOLDER — not the Homepage.
 *
 * The Homepage is Phase 3 (TECHNICAL_ARCHITECTURE.md §23.2) and is built as a
 * motion-port of the approved HiFi. This file exists only so the I-1 foundation
 * has one route to compile, build and validate against; it deliberately carries
 * no homepage module, no curated content and no hero. Replace it wholesale when
 * the Homepage is built — do not grow it.
 */
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  locale="ro"
  rhythm="homepage"
  routeKey="home"
  title="diverse anumite"
>
  <section class="pad-header">
    <p class="eyebrow rv" data-rv="meta">frontend foundation — I-1</p>
  </section>
</BaseLayout>

---
/**
 * `/en/projects` — the English Work Archive (locale route map §11.1, Work archive / EN).
 *
 * The EN segment is `projects`, approved at OD-2 (`DECISIONS_LOG.md` #77). Its
 * set is a locale-scoped subset of the RO one: §7.1 renders "only entities
 * published in the active locale", which `ContentSource` applies before ordering.
 */
import WorkArchive from '../../../components/work-archive/WorkArchive.astro';
import { withheldLocaleResponse } from '../../../lib/i18n/publication';

/* EN is withheld for the initial launch (lib/i18n/publication.ts): no file is emitted. */
const withheld = withheldLocaleResponse('en');
if (withheld) return withheld;
---

<WorkArchive locale="en" />

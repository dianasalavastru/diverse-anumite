---
/**
 * `/en/projects/competitions` — Competitions, EN (locale route map §11.1).
 *
 * Reserved per locale (IA §2.2, F4): `competitions` is unavailable to EN Work
 * Entry slugs, exactly as `concursuri` is to RO ones.
 */
import CuratedCompetitions from '../../../components/work-archive/CuratedCompetitions.astro';
---

<CuratedCompetitions locale="en" />

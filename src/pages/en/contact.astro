---
/**
 * `/en/contact` — the English Contact page (locale route map §11.1, Contact / EN).
 *
 * The RO and EN segments are identical here ("contact" in both), which the
 * frozen route table records deliberately; the prefix is what distinguishes
 * them. Same composition as `/contact`, one prop apart.
 */
import ContactPage from '../../components/contact/ContactPage.astro';
---

<ContactPage locale="en" />
